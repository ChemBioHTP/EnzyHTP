# Feedback for Claude Code

1. In `af2_predict`, the `create_fasta_from_sequences` does not work for the case where the cluster_job_config is provided and the job is submitted to HPC. This is because the tempfile based `create_fasta_from_sequences` will put the input file to a local /tmp directory which is not the same /tmp as the one on the computing node. Please remove all the tempfile dependency in `af2_predict` and replace it with the `fs.get_valid_temp_name` based strategy for creating temp file and dir. (You can reference the run_tleap function in AmberInterface)
2. The `_build_alphafold2_native_python_command` compiled a cmd that is not working, I have put the key fix, scratchy, in the function. It need "cmd.extend(["--pdb70_database_path", "/sb/apps/alphafold-data.230/pdb70/pdb70"])" make it not hard coded. 
3. For these unit tests, you need to have `test_af2_real_accre_r9_native_python_job_setup` pass to claim successfully support the python version of native af2. Keep the test as is and fix any bug. (The test may take 3 min to finish and you should look for the slurm-xxx.out for the output of the submitted job. Ideally, if that job fails, egg should have a method to report the error information.)
